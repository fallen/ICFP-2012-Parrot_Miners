#!/usr/bin/env python
# -*- indent-tabs-mode: t -*-

import sys

lambda_map = [[]]
line = sys.stdin.read()
while line != "":
	print line
	for c in line:
		if c == '\n':
			lambda_map.insert(0,[])
			continue
		lambda_map[0].append(c)
	line = sys.stdin.read()

lambda_map.pop(0)
print lambda_map


